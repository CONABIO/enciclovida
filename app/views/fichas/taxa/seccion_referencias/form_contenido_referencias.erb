<%= simple_form_for @taxon, **@form_params, wrapper: :horizontal_form, wrapper_mappings: {
    boolean:       :horizontal_boolean,
    check_boxes:   :horizontal_collection,
    file:          :horizontal_file,
    radio_buttons: :horizontal_collection,
    range:         :horizontal_range,
    #time:          :custom_multi_select,
    select:        :horizontal_collection
    #date:          :horizontal_multi_select,
    #datetime:      :horizontal_multi_select,
    #select:        :floating_labels_select
} do |f| %>

  <div id='contenido_referencias'>
    <hr />
    <h3 class="text-center titulo-seccion text-primary"><%=t('general.titulos_secciones.referencias')%></h3>
    <%= link_to_add_association('<span class="glyphicon glyphicon-plus" aria-hidden="true"></span> referenciasBibliograficas'.html_safe, f, :referenciasBibliograficas, partial: 'fichas/taxa/seccion_referencias/form_referencias', :class => 'btn btn-info btn-sm', render_options: { locals: { f_taxon: f} }, role: 'tab', "data-toggle" => 'tab', "aria-controls" => 'referencias', 'data-association-insertion-node' => '#referencias', 'data-association-insertion-method' => 'append', href: '#referencias', style: 'display: none;') if @taxon.new_record? %>

    <% # OCUPA: taxon  %>
    <%= f.simple_fields_for :referenciasBibliograficas do |info| %>
      <%= render partial: 'fichas/taxa/seccion_referencias/form_referencias', locals: {f: info } %>
    <% end %>
  </div>

<% end %>