<div class='nested-fields'>

  <%= f.input :descripcion %>
  <%= f.input :descripcion,
              collection: Fichas::Ficha_Ecorregion.all.map { |r| [r.descripcion, r.ecorregionId] },
              :as => :select,
              input_html: {
                  class: 'form-control selectpicker',
                  multiple: true,
                  'data-live-search': 'true',
                  'title': t('general.seleccionar_opciones'),
                  'data-selected-text-format': 'count > 3'
              }
  %>
  <%= link_to_remove_association 'Borrar asociación', f, :class => 'btn btn-danger btn-sm' %><br><br>
</div>
