<h1>Listing especies_regiones</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @especies_regiones.each do |especie_region| %>
      <tr>
        <td><%= link_to 'Show', especie_region %></td>
        <td><%= link_to 'Edit', edit_especie_region_path(especie_region) %></td>
        <td><%= link_to 'Destroy', especie_region, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Especie region', new_especie_region_path %>
