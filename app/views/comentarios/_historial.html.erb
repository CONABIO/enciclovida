<blockquote class="<%= c.es_propietario ? 'op bg-success' : 'respuesta' %>">
  <%= c.comentario %> <br />
  <small><%= c.nombre %> - <%= c.created_at.strftime('%d/%m/%y-%H:%M') %></small>
</blockquote>