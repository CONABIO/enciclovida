<%= tituloNombreCientifico(@pez.especie, link: true).html_safe %>

<h3>
  <% if tiene_permiso?('AdminPeces', true) %>
    <%= link_to '<span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>'.html_safe, edit_pez_path(@pez), 'tooltip-title' => 'Editar', class: 'btn btn-link btn-title pull-right' %>
    <%= link_to '<span class="glyphicon glyphicon-trash" aria-hidden="true"></span>'.html_safe, pez_path(@pez), method: :delete, data: { confirm: '¿Estas seguro?' }, 'tooltip-title' => 'Eliminar', class: 'btn btn-link btn-title pull-right' %>
  <% end %>
</h3>

<div class="row" id="panel-body">
  <% if params[:layout] == '0' && tiene_permiso?('AdminPeces', true) %>
    <%= link_to '<span class="glyphicon glyphicon-cog" aria-hidden="true"></span>'.html_safe, pez_path(@pez),
                'tooltip-title' => 'Administrar', class: 'btn btn-link btn-title pull-right' %>
  <% end %>

  <% if @pez.imagen =~ URI::regexp %>
    <div class="col-sm-6 col-md-7">
      <%= render :partial => 'peces/svg_zonas_marinas_show', locals: {color_zona: @pez.valor_zonas.split('')} %>
    </div>
    <div class="col-xs-8 col-sm-6 col-md-5 col-xs-offset-2 col-sm-offset-0">
      <%= image_tag(@pez.imagen, class: "img-responsive img-thumbnail img-rounded") %>
        <span class="label btn-zona-v">Menor a 5 es recomendable</span><br>
        <span class="label btn-zona-a">De 5 a 19 consume con cautela</span><br>
        <span class="label btn-zona-r">Más de 20 evita</span><br>
    </div>
  <% else %>
    <div class="col-xs-8 col-xs-offset-2">
      <%= render :partial => 'peces/svg_zonas_marinas_show', locals: {color_zona: @pez.valor_zonas.split('')} %>
    </div>
  <% end %>

  <div class="clearfix"></div>

  <div class="col-xs-12">
    <h3>Criterios globales</h3>
    <div class="caracteristicas_pez jumbotron col-xs-12">
      <% @criterios["Características"].each do |c|  %>
        <div class="col-xs-4 col-xs-offset-1">
          <b>
            <%= c[:tipo_propiedad] %>
            <% if Criterio::CON_ADVERTENCIA.include?(c[:nombre]) %>
              <span class="glyphicon glyphicon-exclamation-sign text-danger" aria-hidden="true"></span>
            <% end %>:
          </b>
        </div>
        <div class="col-xs-4">
          <small><%= c[:nombre] %></small>
        </div>
        <div class="col-xs-3">
          <span class="badge"><%= c[:valor] %></span>
        </div>
        <div class="clearfix"></div>
      <% end %>
      <div class="col-xs-9">
        <h4 class="text-center"><b>Suma de criterios globales: </b></h4>
      </div>
      <div class="col-xs-3">
        <h4><span class="badge <%= valorAColor @criterios["suma_caracteristicas"] %>"><%= @criterios["suma_caracteristicas"] %></span></h4>
      </div>
    </div>
  </div>


  <div class="edo_pob_pez col-xs-12">
    <h3>Estado de la población de la especie:<br \>
      <small>(Valoración del edo. de la población por zona + Suma de criterios globales = Valoración por zona)</small>
    </h3>
    <div class="edo_pob_pac_pez jumbotron col-xs-12">
      <% @criterios["Estado poblacional en el Pacífico"].each do |c|  %>
        <div class="col-xs-4 col-xs-offset-1">
          <b>
            <%= c[:tipo_propiedad] %>
            <% if Criterio::CON_ADVERTENCIA.include?(c[:nombre]) %>
              <span class="glyphicon glyphicon-exclamation-sign text-danger" aria-hidden="true"></span>
            <% end %>:
          </b>
        </div>
        <div class="col-xs-4">
          <small><%= c[:nombre] %></small>
        </div>
        <div class="col-xs-3">
          <span class="badge <%= valorAColor c[:valor] %>"><%= c[:valor] %></span>
          <b>+</b>
          <span class="badge <%= valorAColor @criterios["suma_caracteristicas"] %>"><%= @criterios["suma_caracteristicas"] %></span>
          <b>=</b>
          <span class="badge "><%= c[:valor] + @criterios["suma_caracteristicas"]%></span>
        </div>
        <div class="clearfix"></div>
      <% end %>
    </div>
    <div class="edo_pob_gol_pez jumbotron col-xs-12">
      <% @criterios["Estado poblacional en el Golfo de México y caribe"].each do |c|  %>
        <div class="col-xs-4 col-xs-offset-1">
          <b>
            <%= c[:tipo_propiedad] %>
            <% if Criterio::CON_ADVERTENCIA.include?(c[:nombre]) %>
              <span class="glyphicon glyphicon-exclamation-sign text-danger" aria-hidden="true"></span>
            <% end %>:
          </b>
        </div>
        <div class="col-xs-4">
          <small><%= c[:nombre] %></small>
        </div>
        <div class="col-xs-3">
          <span class="badge <%= valorAColor c[:valor] %>"><%= c[:valor] %></span>
          <b>+</b>
          <span class="badge <%= valorAColor @criterios["suma_caracteristicas"] %>"><%= @criterios["suma_caracteristicas"] %></span>
          <b>=</b>
          <span class="badge"><%= c[:valor] + @criterios["suma_caracteristicas"]%></span>
        </div>
        <div class="clearfix"></div>
      <% end %>
    </div>
  </div>

  <div class="col-xs-12">
    <hr />
    <h5 class="text-center"><i>El color de la recomendación es de acuerdo a la suma de los criterios descritos arriba, y varian depediendo la zona</i></h5>
    <h4 class="text-center">
      <span class="label btn-zona-v">Menor a 5 es recomendable</span> - <span class="label btn-zona-a">De 5 a 19 consume con cautela</span> - <span class="label btn-zona-r">Más de 20 evita</span>
    </h4>
  </div>




</div>
