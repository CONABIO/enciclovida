<h1>Administración de especies marinas</h1>
<%= link_to '<span class="glyphicon glyphicon-plus" aria-hidden="true"></span> Agregar nueva especie marina comercial'.html_safe, new_pez_path if params[:action] == 'index' %>
<h2><span class="label label-info"><%= @peces.length%></span></h2>
<table class="table table-striped table-hover table-condensed table-responsive">
  <thead>
  <tr>
    <th>Especie</th>
    <th>Imagen</th>
    <th>Valor total</th>
    <th>Valor zonas</th>
    <% if params[:action] == 'index'  %>
      <th>Administración</th>
    <% end %>
  </tr>
  </thead>

  <tbody>
  <% @peces.each do |pez| %>
    <tr>
      <td>
        <span class="label label-info"><%= pez.especie_id %></span>
        <%= tituloNombresPeces(pez, show: true) %>
      </td>
      <td><%= image_tag(pez.imagen, class: "img-responsive") %></td>
      <td><%= pez.valor_total %></td>
      <td><%= pez.valor_zonas %></td>
      <td>
        <%= link_to '<span class="glyphicon glyphicon-eye-open" aria-hidden="true"></span>'.html_safe, pez_path(pez), title: 'Mostrar'%>
        <%= link_to '<span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>'.html_safe, edit_pez_path(pez), title: 'Editar' %>
        <%= link_to '<span class="glyphicon glyphicon-trash" aria-hidden="true"></span>'.html_safe, pez_path(pez), method: :delete, data: { confirm: '¿Estas seguro?' }, class: 'close', title: 'Eliminar' %>
      </td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to '<span class="glyphicon glyphicon-plus" aria-hidden="true"></span> Agregar nueva especie marina comercial'.html_safe, new_pez_path if params[:action] == 'index' %>

