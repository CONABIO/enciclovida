<%= form_for(@tipo_region) do |f| %>
  <% if @tipo_region.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_region.errors.count, "error") %> prohibited this tipo_region from being saved:</h2>

      <ul>
      <% @tipo_region.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
