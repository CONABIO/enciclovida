<%= form_for(@especie_estatus_bibliografia) do |f| %>
  <% if @especie_estatus_bibliografia.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@especie_estatus_bibliografia.errors.count, "error") %> prohibited this especie_estatus_bibliografia from being saved:</h2>

      <ul>
      <% @especie_estatus_bibliografia.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
