<%= form_for(@adicional) do |f| %>
  <% if @adicional.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@adicional.errors.count, "error") %> prohibited this adicional from being saved:</h2>

      <ul>
      <% @adicional.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
