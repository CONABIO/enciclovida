<% if page.nil? %>
  <div class="d-flex flex-column flex-md-row justify-content-between mb-3">
    <% icono = {'photo' => 'camera', 'video' => 'film'} %>
    <i class='fa fa-<%=icono[type]%>'></i>
    <i class ='bdi-ev-icon'></i>
    <i class ='conabio_completo-ev-icon'></i>
  </div>
  <div class="paginado-media d-flex">
<% end %>

<% if @fotos.any? %>
  <button type="button" class="btn btn-sm btn-light btn-outline-dark"><i class="fa fa-hand-o-left"></i></button>
  <button type="button" class="btn btn-sm btn-light btn-outline-dark"><i class="fa fa-hand-o-right"></i></button>
  <% @fotos.each do |foto| %>
    <%= imprime_media_bdi(foto, 'photo') %>
  <% end %>
<% else %>
  <p>Lo sentimos, no contamos con una imágen para esta especie, <a href="https://www.biodiversidad.gob.mx/recursos/bancoimg.html">¿quieres contribuir proporcionando una imágen?</a></p>
<% end %>

<% if page.nil? %>
  <!--div class="text-center paginado_2"></div-->
  </div>
<% end %>

