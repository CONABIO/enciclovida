<div id="paginado_fotos">

	<% if @pagina.blank? && @fotos.any? %>
	  <div class="text-center paginado_1"></div>
	<% end %>

  <div class="result-img-container" style="float: left;">
    <i class ='conabio_bdi-ev-icon'></i>
  </div>

  <% if @fotos.any? %>
    <% @fotos.each do |foto| %>
      <div class="result-img-container" id="fotos_paginado_container">
        <%= link_to (image_tag foto.medium_url), foto.native_page_url, target: :_blank %>
      </div>
    <% end %>
  <% else %>
    <p>Lo sentimos, no contamos con una imágen para esta especie, <a href="http://www.biodiversidad.gob.mx/recursos/bancoimg.html">¿quieres contribuir proporcionando una imágen?</a>
		</p>
  <% end %>



	<% if @pagina.blank? && @fotos.any? %>
	  <div class="text-center paginado_2"></div>

	  <script>
		  paginado_fotos(<%= @paginas || 1 %>, 1);
	  </script>
	<% end %>

</div>