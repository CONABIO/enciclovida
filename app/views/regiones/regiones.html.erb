<%= select_tag("distribucion_nivel_#{@nivel+1}", options_for_select(@regiones.collect {|r| [r.nombre_region.primera_en_mayuscula, r.id] }), prompt: 'Todos', class: "form-control") if @regiones.present? %>
