<% if params[:pagina] == 1 %>
    <ul class="list-group">
<% end %>

<% @region_mapa.each do |region| %>
    <li class="list-group-item">
      <%# ancestros = region.ancestors.reverse.map(&:nombre_region).join(', ') %>
      <%= link_to "#{region.entidad}", nil, region_id: region.entid, class: 'region_id' %>
    </li>
<% end %>

<% if params[:pagina] == 1 %>
    </ul>
<% end %>