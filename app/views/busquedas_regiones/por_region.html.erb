<% content_for(:extracss) do %>
  <!-- Latest compiled and minified CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-select@1.13.9/dist/css/bootstrap-select.min.css">
<% end %>

<% content_for(:extrajs) do %>
  <!-- Latest compiled and minified JavaScript -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap-select@1.13.9/dist/js/bootstrap-select.min.js"></script>
<% end %>

<% content_for(:delayedjs) do %>
  <script data-turbolinks-track="true">
      especies = null;
      especies_con_filtro = null;

      opciones = {};
      opciones.solo_coordenadas = true;
      opciones.datos = {};
      opciones.datos.taxones = {};
      opciones.filtros = {};
      opciones.filtros.pagina = 1;

      cargaMapaYoverlays();
  </script>

  <%= javascript_include_tag 'busquedas/region/por_region_extra', 'data-turbolinks-track' => true %>
<% end %>

<% content_for(:extra_title_header) do %>
  <%=  render :partial => 'shared/tituloBuscador', locals: {icon_class: 'busqueda-region', titulo: 'Búsqueda por región', subtitulo: 'Selecciona una región (doble clic)'} %>
<% end %>

<div id="contenedor_mapa" class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
  <%= render partial: 'sidebar' %>
  <div id="map" class="sidebar-map"></div>
</div>

<button type="button" class="btn pull-right" data-toggle="modal" data-target="#modal-descargas-region">
  Descargar &nbsp;<span class="glyphicon glyphicon-save"></span>
</button>

<%= render partial: 'descarga' %>

<div class="row" id="contenedor_especies" class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
</div>