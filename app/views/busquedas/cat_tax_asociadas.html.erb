<%= select_tag :nivel, options_for_select(Busqueda::NIVEL_CATEGORIAS, '>='), :class => 'form-control' %>

<%= select_tag :cat, options_for_select(
                           @especie.cat_tax_asociadas.collect{ |u| [u.nombre_categoria_taxonomica, u.nivel] },
                           @especie.cat_tax_asociadas.collect{ |u| u.nivel if u.nombre_categoria_taxonomica == 'especie' }), :class => 'form-control' %>