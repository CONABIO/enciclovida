<h1>Listing estatuses</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @estatuses.each do |estatuse| %>
      <tr>
        <td><%= link_to 'Show', estatuse %></td>
        <td><%= link_to 'Edit', edit_estatuse_path(estatuse) %></td>
        <td><%= link_to 'Destroy', estatuse, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Estatus', new_estatuse_path %>
